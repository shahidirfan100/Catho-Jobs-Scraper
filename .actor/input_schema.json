{
  "title": "Catho Jobs Scraper",
  "description": "Extract job listings from Catho.com.br - Brazil's leading job platform.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "startUrl": {
      "title": "Start URL",
      "type": "string",
      "description": "Catho search URL to scrape. If set, overrides keyword/location.",
      "editor": "textfield",
      "example": "https://www.catho.com.br/vagas/?q=desenvolvedor&cidade=São Paulo"
    },
    "keyword": {
      "title": "Search Keywords",
      "type": "string",
      "description": "Job title or skills to search (e.g., 'desenvolvedor', 'engenheiro').",
      "editor": "textfield",
      "prefill": "",
      "default": "",
      "example": "desenvolvedor"
    },
    "location": {
      "title": "City or State",
      "type": "string",
      "description": "City or state to search (e.g., 'São Paulo', 'Rio de Janeiro').",
      "editor": "textfield",
      "example": "São Paulo"
    },
    "lastDays": {
      "title": "Date Filter",
      "type": "string",
      "description": "Filter jobs by posting date.",
      "editor": "select",
      "enum": ["anytime", "today", "2days", "3days", "week", "month"],
      "enumTitles": ["Anytime", "Today", "Last 2 Days", "Last 3 Days", "Last Week", "Last Month"],
      "default": "anytime",
      "prefill": "anytime"
    },
    "results_wanted": {
      "title": "Maximum Jobs to Extract",
      "type": "integer",
      "description": "Maximum number of job listings to collect.",
      "minimum": 1,
      "default": 50,
      "prefill": 50,
      "editor": "number"
    },
    "proxyConfiguration": {
      "title": "Proxy Configuration",
      "type": "object",
      "description": "Recommended: Use Apify Proxy to avoid blocking.",
      "editor": "proxy",
      "default": {
        "useApifyProxy": true,
        "apifyProxyGroups": [
          "RESIDENTIAL"
        ]
      },
      "prefill": {
        "useApifyProxy": true,
        "apifyProxyGroups": [
          "RESIDENTIAL"
        ]
      }
    }
  },
  "required": []
}